/** @type {import('next').NextConfig} */
const nextConfig = {
  // 🚀 ຍ້າຍມາໄວ້ດ້ານນອກສຸດ (Root level) ຕາມມາດຕະຖານຂອງ Next.js
  allowedDevOrigins: ['192.168.43.100', 'localhost:3000']
};

export default nextConfig;
